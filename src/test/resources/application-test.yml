logging:
  level:
    es.upm.miw: DEBUG

test:
  api:
    apaw-user: http://localhost:8080/api/apaw-user
    apaw-practice: http://localhost:8080/api/apaw-practice

spring:
  cloud:
    openfeign:
      client:
        config:
          users:
            connectTimeout: 5000
            readTimeout: 10000
